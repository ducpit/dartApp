export interface Game {
    activePlayer: number,
    gameState: number,
    inCondition: number,
    outCondition: number,
    finish: string,
    number: number,
    type: string,
    difference: number,
}